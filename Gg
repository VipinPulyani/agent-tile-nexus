You are an expert T-SQL parser.

Input: A T-SQL stored procedure as plain text.

Task:
- Break it into a JSON array of logical chunks **in order**.

For each chunk include:
- type: one of [header, set_option, parameters, declare, transaction, try, catch, cte, dml, exec, error]
- start_line, end_line (1-based index from the original input)
- exact SQL text

Rules:
- Preserve order exactly.
- Don’t rewrite or interpret SQL.
- Combine consecutive DECLARE statements into one block if they are contiguous.

Output strict JSON array.
